syntax = "proto2";
package gz_geometry_msgs;

import "Header.proto";
import "vector3d.proto";

// Extends the Gazebo Vector3d message type to
// include a header.
message Vector3dStamped
{
  required gz_std_msgs.Header header = 1;
  required gazebo.msgs.Vector3d position = 2;
}