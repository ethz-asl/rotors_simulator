syntax = "proto2";
package gz_geometry_msgs;

import "Vector3.proto";

// Wrench includes both force and torque information about a joint.
message Wrench
{
  required Vector3 force = 1;
  required Vector3 torque = 2;
}