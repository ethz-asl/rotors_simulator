syntax = "proto2";
package gz_geometry_msgs;

// Quaternion message type which designed to be used
// in other message types
message Quaternion {
  required double x = 1;
  required double y = 2;
  required double z = 3;
  required double w = 4;
}