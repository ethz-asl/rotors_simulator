syntax = "proto2";
package gz_geometry_msgs;

import "Position.proto";
import "Quaternion.proto";

// Pose message type which designed to be used
// in other message types
message Pose
{
  required Position position = 1;
  required Quaternion orientation = 2;
}